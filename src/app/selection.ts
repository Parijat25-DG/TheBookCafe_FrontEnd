import { CartList } from "./cartList";

export class Selection{

    cartId:number;
    cartList:any;
    totalAmt:number;
    paymentMode:number;

    constructor(
        cartId:number,cartList:any, totalAmt:number,paymentMode:number
    ){

    }
}