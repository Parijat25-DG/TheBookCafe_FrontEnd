import { ClassTypePipe } from './class-type.pipe';

describe('ClassTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ClassTypePipe();
    expect(pipe).toBeTruthy();
  });
});
