import { ClubGenrePipe } from './club-genre.pipe';

describe('ClubGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new ClubGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
